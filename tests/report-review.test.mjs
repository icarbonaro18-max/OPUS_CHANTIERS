import test from 'node:test';import assert from 'node:assert/strict';import {hasArchivedFinalReport,isFinalProjectReport} from '../lib/report-review.js';import {progress,targetStage} from '../lib/project-progress.js';
const report={id:'r',projectId:'p',reportKind:'intervention',status:'terminee',pdfSavedAt:'2026-09-19T14:00:00Z',reportSubmittedAt:'2026-09-19T13:59:00Z'};
test('archived end of intervention repairs missing review transition without needing a new report',()=>{const data={interventions:[report],events:[],projectTracking:[],people:[]};assert.equal(isFinalProjectReport(report),true);assert.equal(targetStage({category:'02'},progress(data,'p')),'03');assert.equal(targetStage({category:'04'},progress(data,'p')),'04');});
test('daily draft missing PDF or report for another project never triggers review',()=>{for(const r of [{...report,reportKind:'journee'},{...report,status:'en_cours'},{...report,pdfSavedAt:null},{...report,projectId:'other'}])assert.equal(hasArchivedFinalReport({interventions:[r]},'p'),false);});
test('office continuation after report wins; newer final report stays in review',()=>{const data={interventions:[report],projectTracking:[{id:'p',continueWork:true,history:[{action:'Poursuite validée',at:'2026-09-19T15:00:00Z'}]}],events:[],people:[]};assert.equal(targetStage({category:'03'},progress(data,'p')),'02');data.interventions=[{...report,pdfSavedAt:'2026-09-20T14:00:00Z',reportSubmittedAt:'2026-09-20T13:59:00Z'}];assert.equal(targetStage({category:'03'},progress(data,'p')),'03');});
