import test from 'node:test';import assert from 'node:assert/strict';import {fillMissingCoordinates,autoFillCoordinates} from '../lib/project-autofill.js';
test('fills empty coordinates without replacing corrected client or mixing billing and site addresses',()=>{const m={devis:'001168',client:'Syndic corrigé',adresse:'',billingAddress:'Agence',tasks:[{title:'Manuel'}]};const n=fillMissingCoordinates(m,{devis:'1168',client:'Autre',adresse:'10 rue du chantier'},{});assert.equal(n.client,m.client);assert.equal(n.adresse,'10 rue du chantier');assert.equal(n.billingAddress,'Agence');assert.deepEqual(n.tasks,m.tasks);assert.equal(m.adresse,'');assert.throws(()=>fillMissingCoordinates(m,{devis:'001169',adresse:'Autre'},{}),/correspond pas/);});
test('multiple PDFs require selection and complete records do not trigger downloads',async()=>{let downloads=0;const g={children:async()=>[{id:'a',name:'Feuille1.pdf'},{id:'b',name:'Feuille2.pdf'}],bytes:async()=>downloads++};assert.equal(await autoFillCoordinates(g,{id:'p'},{devis:'001168'}),null);assert.equal(await autoFillCoordinates(g,{id:'p'},{devis:'001168',client:'Syndic',adresse:'Rue'}),null);assert.equal(downloads,0);});
